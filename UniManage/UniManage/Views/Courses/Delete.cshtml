@model UniManage.Models.CourseModel
@{
    ViewData["Title"] = "Delete Course";
}
<div class="max-w-lg mx-auto">
    <div class="page-card">
        <div class="flex items-start gap-4 mb-5">
            <div class="w-12 h-12 rounded-xl bg-red-50 flex items-center justify-center flex-shrink-0">
                <i class="fa-solid fa-triangle-exclamation text-red-600 text-xl"></i>
            </div>
            <div>
                <h2 class="text-base text-slate-800">Delete Course</h2>
                <p class="text-sm text-slate-500 mt-0.5">This action is permanent and cannot be undone.</p>
            </div>
        </div>
        <div class="bg-red-50 border border-red-100 rounded-lg p-4 mb-5 text-sm space-y-2">
            <p><span class="text-slate-500"><i class="fa-solid fa-book-open mr-1"></i>Title:</span> <strong class="text-slate-800">@Model.Title</strong></p>
            <p><span class="text-slate-500"><i class="fa-solid fa-tag mr-1"></i>Code:</span> <span class="font-mono">@Model.Code</span></p>
            <p><span class="text-slate-500"><i class="fa-solid fa-building mr-1"></i>Department:</span> @Model.Department?.Name</p>
        </div>
        <form asp-action="Delete" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <div class="flex gap-3">
                <button type="submit" class="text-sm bg-red-600 hover:bg-red-700 px-6 py-1 text-white rounded-lg border border-red-600"><i class="fa-solid fa-trash"></i> Delete Permanently</button>
                <a asp-action="Index" class="btn-secondary py-1"><i class="fa-solid fa-arrow-left"></i> Cancel</a>
            </div>
        </form>
    </div>
</div>
